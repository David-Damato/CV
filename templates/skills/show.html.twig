{% extends 'base.html.twig' %}

{% block title %}Skills{% endblock %}

{% block body %}
    <h1>Skills</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ skill.type }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill.name }}</td>
            </tr>
            <tr>
                <th>Description1</th>
                <td>{{ skill.description1 }}</td>
            </tr>
            <tr>
                <th>Description2</th>
                <td>{{ skill.description2 }}</td>
            </tr>
            <tr>
                <th>Description3</th>
                <td>{{ skill.description3 }}</td>
            </tr>
            <tr>
                <th>Description4</th>
                <td>{{ skill.description4 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('skills_index') }}">back to list</a>

    <a href="{{ path('skills_edit', {'id': skill.id}) }}">edit</a>

    {{ include('skills/_delete_form.html.twig') }}
{% endblock %}
